# proton-cachyos uses ntsync for Win32 synchronisation primitives when
# /dev/ntsync exists. The distro's own Proton packages load the module the same
# way; this copy is named after the package so both can be installed at once.
ntsync
